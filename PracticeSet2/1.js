let age = 25;

// Check if age is between 18 and 30 (inclusive)
if (age >= 18 && age <= 30) {
  console.log("Age is between 18 and 30.");
} else {
  console.log("Age is outside the range of 18 to 30.");
}
